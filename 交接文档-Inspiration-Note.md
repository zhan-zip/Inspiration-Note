# Inspiration-Note 项目交接文档

## 一、项目概述

一款简约黑白的**个人灵感记录 + 任务管理 PWA**，帮助用户随手记录灵感、将灵感转为任务、用日历安排任务、可视化每日任务量、复盘已完成任务。解决「功能想做但懒得做，随手记了不知道忘哪去」的痛点。

**目录：** `D:\Desktop\test\claude-test\inspiration-note\`

**仓库：** <https://github.com/zhan-zip/Inspiration-Note>（空仓库，代码待推送）

**运行形态：** PWA，手机本地运行（IndexedDB 本地存储，离线可用，可添加到主屏）

**开发周期：** 2026 年 8 月起，个人工具持续迭代

**当前状态：** P0、P1 完成；任务/灵感关系与导航交互已重构完善（任务列表=项目+无项目任务条目，独立项目页取消，右拉返回/开抽屉）；P2（PWA 图标 + 部署）待实施

---

## 二、核心需求

1. **灵感记录**（入口 / 默认主页）：随手记灵感，可「一键转为任务」
2. **任务管理**：任务可归入项目 / 新建项目 / 无项目；可分周期安排到日历
3. **日历安排**：任务安排到某天，支持**周期**（每周/每月/每年）或**单次**
4. **今日完成**：当天已安排任务，打钩记录完成
5. **任务量可视化**：日历某天任务数 → 白/灰/黑（**3 个=灰，5 个=黑**）
6. **复盘页**：按周/月查看已完成任务，按项目分类，带项目小字角标
7. **项目管理**：任务列表页提供「新建任务 / 新建项目」入口，点击任务上的**项目名**进入该项目任务列表页管理（增删改移）
8. **回收站**：所有删除均走**软删除**进回收站，可恢复或彻底删除

---

## 三、界面与交互设计（已确认）

### 3.1 导航方案（2026-08-02 确认 · 推翻底部 Tab）

- **不使用底部 Tab**
- **主页 = 灵感页**（开屏默认）
- **左滑** → 呼出左侧**页面导航列表**：灵感 / 任务列表 / 日历 / 今日 / 复盘 / 项目 / 回收站
- **点击页面名** → 列表收回，右侧主视图切换为对应页面
- 从**任意页面**左滑均可呼出导航列表（防止进入某页后无法离开）
- **灵感页内部**：**下拉** → 展开全部灵感记录列表；**上拉** → 收起回到记录输入框

### 3.2 列表项交互（2026-08-03 确认 · 左滑多操作 + 软删除）

- 列表项**向左滑** → 露出操作按键（可多个）→ 点击按键 → 弹确认 → 执行
- **任务**左滑：**编辑 + 删除**；单击任务**无操作**
- **灵感**左滑：**转为任务 + 删除**；灵感转任务后**从灵感列表消失**（列表仅显示未转灵感）
- **项目删除**：弹提示 **「将连带删除其下所有任务」** + 建议先将有用任务移出 → 确认后项目与其下所有任务**一起删除**
- 所有删除均走**软删除** → 进**回收站**，可恢复或彻底删除（详见 3.9）

### 3.3 页面 1：灵感页（默认主页 / 开屏）

```
┌─────────────────────────┐
│  记录一个灵感吗？        │  ← 一行大字提问
│  ┌───────────────────┐  │
│  │ 输入灵感...        │  ← 随手记输入框
│  └───────────────────┘  │
│                        │
│  （下拉展开灵感记录）    │
│   · 灵感1  [转为任务]   │
│   · 灵感2  [转为任务]   │
└─────────────────────────┘
```

- 打开 App 默认进入此页
- 顶部一行大字：**「记录一个灵感吗？」**
- 中间输入框，随手记灵感
- **下拉**从顶部盖下「所有灵感」列表（盖住输入框）；**上拉**收回回到记录
- 每条灵感：**大字内容 + 创建时间小字**
- 灵感**左滑**：**转为任务**（选已有项目/新建/无项目）或**删除**
- 灵感转任务后**从灵感列表消失**（灵感和任务彼此独立）

**灵感 → 任务（转化时选项）**：
- 归入**已有项目**（如「食光记」）
- 或**新建项目**
- 或**无项目**

> 设计考量：灵感和任务分开对用户不友好，所以开屏即记录；转任务要简单（用户主动写「待办」不便），灵感记录后可选是否加入任务。

### 3.4 页面 2：任务列表页（2026-08-03 重构）
- 展示**所有任务**（含已安排日历的），按创建时间倒序
- 顶部提供「**新建任务**」「**新建项目**」两个入口
- 有项目归属的任务右侧显示**项目名**，点击项目名 → 进入该项目任务列表页
- 任务左滑：**编辑**（改名/移动项目）或**删除**
- 单击任务**无操作**

### 3.5 页面 3：日历页
- 把任务安排到某天，支持**周期**（每周 / 每月 / 每年）或**单次**
- 集成**任务量可视化**：某天任务数白/灰/黑（3 灰 / 5 黑）

### 3.6 页面 4：今日页
- 当天已安排的任务（含周期任务自动匹配）
- 打钩标记完成

### 3.7 页面 5：复盘页
- 按周 / 按月查看已完成任务
- 按项目分类
- 每个任务下方一行小字标注所属项目（如「食光记」）

### 3.8 页面 6：项目任务列表页（2026-08-03 新增，替代原「项目管理页」）
- 由任务列表页点击任务上的**项目名**进入（路由 `/project/:id`）
- 页面**只显示该项目的任务**，支持：添加任务、编辑任务、删除任务（左滑编辑/删除）
- 项目管理：**重命名项目**、**删除项目**（弹提示连带删除其下所有任务 + 建议先移出有用任务）
- 顶部「← 返回」回到任务列表
- **独立「项目管理页」已取消**，导航列表不含「项目」

### 3.9 页面 7：回收站页（2026-08-02 新增）
- 列出所有已软删除的内容（灵感 / 任务 / 项目）+ **删除时间**
- 每条可**恢复**：清空删除标记回原位；项目恢复时其下任务一并恢复
- 每条可**彻底删除**：物理从 IndexedDB 删除，**不可恢复**（删除前弹确认）

---

## 四、技术栈（已确认 + 补充）

| 技术 | 用途 | 说明 |
|------|------|------|
| **Vue 3 + Vite** | 前端框架 | 用户已熟练掌握（食光记同栈），零学习成本 |
| **Pinia** | 状态管理 | Vue 生态标配 |
| **vue-router** | 页面路由 | ⚠️ 补充依赖：页面切换需要 |
| **vite-plugin-pwa** | PWA 能力 | 可安装、离线缓存、添加到主屏 |
| **idb** | IndexedDB 封装 | ⚠️ 补充依赖：本地数据存储简洁可靠 |
| **纯 CSS 黑白主题** | 界面 | 简约黑白，**以白为主**，CSS 变量定义黑白灰阶 |
| **GitHub Pages** | 免费托管 | HTTPS 自带，满足 PWA 可安装要求 |

**无需后端服务器**——纯前端 + 本地存储，完全离线运行。

---

## 五、数据模型（本地 IndexedDB，5 张表）

| store | 主键 | 关键字段 | 软删除字段 |
|-------|------|----------|-----------|
| **ideas**（灵感） | id | content, created_at, status（未转/已转） | `deleted_at` |
| **projects**（项目） | id | name, created_at | `deleted_at` |
| **tasks**（任务） | id | name, project_id（可空）, status（待办/已完成）, created_at | `deleted_at` |
| **schedules**（日程） | id | task_id, date, type（once/weekly/monthly/yearly） | 无（随任务删除状态过滤） |
| **completions**（完成记录） | id | task_id, date | 无（复盘页用） |

**软删除规则**：
- `deleted_at` 为 `null` = 正常；有值 = 已删除，进回收站，正常页面不显示
- 任务软删除后，其 `schedules` / `completions` 保留但随任务状态被过滤；任务恢复后一并恢复生效
- 项目软删除时其下任务一并软删除；项目恢复时任务一并恢复

---

## 六、部署方案（GitHub Pages 免费托管）

用户不太熟悉 Git/GitHub 部署，之前用过 Zeabur（付费），本方案用 **GitHub Pages 免费托管**：

1. 本地 `git init` + 关联远程：`git remote add origin https://github.com/zhan-zip/Inspiration-Note.git`
2. 开发完成 → `git add` + `git commit` + `git push origin main`（或 master，视仓库默认分支）
3. 用 **GitHub Actions** 配置自动构建（`vite build` + 部署到 Pages），或直接在仓库 Settings → Pages 里配
4. 发布后得到 `https://zhan-zip.github.io/Inspiration-Note/` 地址
5. 手机浏览器打开该地址 → 添加到主屏 → 离线使用 ✅

> ⚠️ **注意**：GitHub Pages 部署的是**构建产物**，需要配好 Vite 的 `base: '/Inspiration-Note/'`（子路径部署），否则静态资源路径 404。这是 PWA 部署最常见的坑。

---

## 七、启动方式

| 命令 | 用途 |
|------|------|
| `npm install` | 安装依赖 |
| `npm run dev` | 本地开发（默认 http://localhost:5173） |
| `npm run build` | 生产构建（产物含 PWA service worker） |
| `npm run preview` | 本地预览构建产物 |

---

## 八、开发计划

分阶段实施，详见同目录下 `实施计划-Inspiration-Note.md`。总顺序：

> 初始化骨架 → 数据层 → 灵感页 → 任务列表 → 日历+可视化 → 今日 → 复盘 → 项目管理 → 回收站 → PWA+部署

---

## 九、已知问题及注意事项

| 风险 | 说明 | 对策 |
|------|------|------|
| **base 路径 404** | Pages 子路径部署，资源路径不对会全白 | 一开始就配 `base: '/Inspiration-Note/'` |
| **目录非空初始化** | 当前目录已有文档，脚手架可能提示 | 选「忽略现有文件」，保留 md 文档 |
| **IndexedDB 结构变更** | 后续改数据模型需升版本号 | db.js 统一管理版本与迁移 |
| **周期任务日期匹配** | 每周/每月/每年的匹配逻辑易错 | 单独封装 `getTasksForDate()` 统一处理并测试 |
| **PWA 图标缺失** | 无图标无法添加到主屏 | 部署前补真图标（192/512/maskable） |
| **软删除过滤遗漏** | 各页面查询需统一过滤 `deleted_at` | store 层统一封装，页面不直接碰 DB |

---

## 十、依赖

```
vue@3, vue-router@4, pinia@2
vite, @vitejs/plugin-vue
vite-plugin-pwa, idb
```

---

## 十一、快速排查清单

| 现象 | 检查 |
|------|------|
| 页面空白 | `npm run dev` 是否正常？`base` 是否配置？ |
| PWA 无法安装 | manifest 图标（192/512/maskable）是否齐全？ |
| 数据不显示 | IndexedDB 中数据是否被软删除（`deleted_at`）？ |
| 数据丢失/报错 | IndexedDB 版本是否与代码一致？ |
| 部署后资源 404 | `vite.config.js` 的 `base: '/Inspiration-Note/'` 是否正确 |

---

**最后更新：** 2026-08-03 21:40\
**项目路径：** `D:\Desktop\test\claude-test\inspiration-note\`\
**开发周期：** 个人工具，持续迭代

---

## 十二、开发迭代记录

> 以下按时间顺序记录需求确认、设计决策、实施进度，供了解演进过程参考。

### 十二.1、2026-08-02 23:20 初始交接文档创建（第一轮）

- 明确需求：灵感记录 + 任务管理 PWA，7 项核心需求，6 个页面
- 确认技术栈：Vue 3 + Vite + Pinia + vite-plugin-pwa + IndexedDB + 黑白主题 + GitHub Pages
- 确认数据模型 5 张表、部署方案（Pages 子路径、base 配置）

### 十二.2、2026-08-02 23:33 导航方案大改 + 删除方案确认（2026-08-02 更新）

- **导航推翻底部 Tab**：改为主页（灵感页）**左滑呼出页面导航列表**，点击页面名切换主视图；灵感页内下拉展开记录 / 上拉收起
- **删除改右滑交互**：列表项右滑露出删除键 → 点击 → 弹确认 → 删除
- **删除统一走软删除**：新增 `deleted_at` 字段 + **回收站页面**（可恢复 / 彻底删除）
- **项目删除连带其下任务**：删除时弹提示说明 + 建议先移出有用任务
- 交接文档格式对齐 Food-Time 项目 summary（编号章节 + 元信息 + 迭代记录 + 测试记录）

### 十二.3、2026-08-02 23:43 P0 阶段完成（初始化骨架 + 数据层 + 四大核心页面）

- **项目初始化**：因 Vite 脚手架在非空目录交互取消，改为手动搭建 Vue3+Vite 项目结构；依赖 vue3 / vue-router5 / pinia4 / vite8 / vite-plugin-pwa / idb
- **基础骨架**：黑白主题 CSS 变量、`base: '/Inspiration-Note/'`、PWA manifest（图标待 P2 补）、7 个路由 + **侧滑抽屉导航**（主页=灵感页，左滑呼出导航列表，点击页面名切换）
- **数据层**：IndexedDB 5 张表（ideas/projects/tasks/schedules/completions）+ `deleted_at` 软删除 + 5 个 Pinia store（删除/恢复/彻底删除/项目级联删除恢复）
- **页面**：灵感页（下拉展开记录/上拉收起 + 一键转任务 + 右滑删除）、任务列表（待办池，新增/编辑/删除）、日历（单次+周期安排 + 白灰黑任务量可视化 ≤2/3-4/≥5）、今日（当天任务自动聚合 + 打钩完成）
- **自测**：`npm run build` 通过；date.js 周期匹配逻辑 12 项单元测试通过；dev server 运行无报错
- **待办**：P1 复盘页 / 项目管理页 / 回收站页；P2 PWA 图标 + 部署上线

### 十二.4、2026-08-03 00:25 交互打磨与缺陷修复（P0 稳定）

- **手势系统全面改造**：touch 事件 → Pointer 事件（鼠标/触摸/触控笔统一）；`e.buttons===0` 才响应（鼠标 hover 不再误触）；抽屉导航改为「**向右拉出 / 向左推回**」且全程跟随手指
- **灵感列表重构为全屏覆盖层**：下拉从顶部盖下来**盖住输入框**，上拉收回；每条灵感 **大字内容 + 小字（项目/时间）**；**项目归属在转任务时回填**（已有/新建项目名正确显示）
- **删除改为左滑 + 自绘确认浮窗**：列表项向左滑露出删除键，点击弹自绘 ConfirmDialog（替换浏览器 confirm）
- **手势冲突修复**：灵感列表空白区可拖动抽屉，仅在灵感项上拦截（拖选项不带动抽屉）；拖拽范围 clamp 防拖出屏幕
- **数据层测试**：新增 `test-store.mjs`（fake-indexeddb 实测 3 场景：已有/新建/无项目回填，全部通过）
- **遗留**：快速大幅拖拽偶发卡顿（个人使用可接受，暂不修复）

### 十二.5、2026-08-03 00:28 P1 阶段完成（复盘 / 项目管理 / 回收站）

- **复盘页**：周/月切换 + 上/下周期导航 + 回今天；按项目分组展示完成记录，任务带项目小字角标
- **项目管理页**：项目卡片区块 + 点击展开项目内任务；无项目任务单独区块放下方；**增删改移**（新建/重命名/编辑任务/移动项目）；删除项目弹**连带提示**（建议先移出有用任务）+ 确认后连带软删
- **回收站页**：软删除内容分「项目/任务/灵感」三区 + 删除时间；可**恢复**（项目恢复连带恢复其下任务）或**彻底删除**（物理删除，清理其日程/完成记录）
- **数据层测试**：新增 `test-cascade.mjs` 级联测试（项目软删连带、恢复连带、purge 清理）4 项全通过；回填测试 3 项全通过
- **待办**：P2 PWA 图标 + GitHub Pages 部署

### 十二.6、2026-08-03 00:39 任务与灵感关系重构

- **灵感/任务分离**：灵感转任务后**从灵感列表消失**（列表仅显示未转灵感），灵感与任务彼此独立
- **任务列表页重构**：展示**所有任务**；顶部「新建任务」「新建项目」两个入口；有项目归属的任务显示**项目名**，点击跳转该项目任务列表页
- **新增项目任务列表页**（`/project/:id`）：仅显示该项目任务，支持添加/编辑/删除任务、重命名/删除项目（删除连带任务 + 提示）
- **取消独立「项目」页**：导航列表移除「项目」，项目管理整合进任务列表 + 项目任务页
- **左滑多操作**：任务左滑「编辑 + 删除」；灵感左滑「转为任务 + 删除」；单击不再触发编辑
- **测试**：数据层 4 场景（新增"转任务后消失"断言）全通过；级联测试 4 项全通过

### 十二.7、2026-08-03 01:04 任务列表与导航交互完善

- **任务列表重构**：展示「**项目条目 + 无项目任务条目**」混合列表（【项目 X】/【任务 Y】）；项目条目点击进入该项目任务列表页；左滑：项目=重命名/删除，任务=编辑/删除
- **SwipeItem 多按钮**：支持多个操作按钮（宽度自适应）；组件内部拦截 pointer 冒泡——操作列表项左滑/右滑时**不带动抽屉与列表层**
- **项目任务页导航**：移除返回按钮，改为**右拉（自左向右）返回**——复用 TaskView 作下层，右拉时任务列表像抽屉一样从左侧**衔接拉出**（左 70% 任务列表 + 右 30% 页面边缘），松手滑满并切回任务列表
- **抽屉方向统一**：仅**右拉（自左向右）**打开抽屉；左滑在任务列表不触发任何动作；抽屉开着时左滑关闭
- **测试**：构建通过；数据层 4 场景 + 级联 4 项全过

### 十二.8、2026-08-03 19:18 前端优化第一批（P0 三项，基于 impeccable 审计）

- **保存灵感成功反馈**：新增全局 toast 系统（`src/toast.js`），保存灵感后显示「已记录 ✓」
- **导航可见入口**：按用户偏好**不做按键**，改为**左缘细条引导**（脉冲动画，纯视觉提示可右拉），「右拉滑出」手势不变；页面顶部留白恢复原样
- **抽屉宽度与遮罩**：主视图位移改 70%（抽屉全露）；新增半透明遮罩，点击关闭抽屉
- 形成《前端优化计划-Inspiration-Note.md》（审计 23.5/40，3 批 14 项问题）
- **浏览器自测通过**：toast 显示、≡ 开抽屉、抽屉全露 + 遮罩点击关闭
- **待办**：第二批 P1-P2 五项（左滑引导 / 触控热区 / 转任务简化 / 无障碍 / 空状态文案）

### 十二.9、2026-08-03 19:52 抽屉手势健壮性修复（鼠标拖拽）

- **根因**：抽屉滑出后盖住鼠标位置，pointer 事件目标切换，监听挂在 main 上收不到 → 抽屉停在半截、导航项在屏幕外；在抽屉上左拖也无法关闭
- **修复**：拖拽监听移到**最外层 .app**（任意区域拖拽都能收到）；**判定为拖拽后才 setPointerCapture** 锁定指针（避免普通点击的 click 被重定向）；松手改用**累计位移 totalDx** 决策开关；拖动灵敏度调至 28% 视口
- **浏览器实测**：鼠标拖拽打开/关闭抽屉正常、点击导航项跳转正常
- **补充（19:55）**：App 不再接管子页手势——项目任务页**右拉衔接拉出任务列表并返回**由 ProjectTasksView 自身处理，左滑无操作（不跳转、不拉抽屉）

### 十二.10、2026-08-03 20:05 前端优化第二批（P1-P2 五项）

- **转任务流程简化**：默认「无项目」一步确认转任务，归入项目改为可选
- **触控热区 ≥44px**：全局 `.btn` 统一最小 44×44px 点击区
- **空状态文案断句**：今日/复盘空状态加句号
- **左滑操作引导**：首次进入 toast 提示「左滑条目可编辑/删除」（localStorage 一次性）
- **隐藏内容无障碍**：抽屉 / SwipeItem 操作区 / 灵感覆盖层收起时 `aria-hidden`
- **浏览器实测**：五项全部通过
- **修复（20:12）**：转任务弹窗项目下拉框仅在选「归入已有项目」时显示（此前一直显示，选项目但模式停在「无项目」导致任务未归项目）；实测项目归属正确

### 十二.11、2026-08-03 20:35 任务列表优化（4 项用户需求）

- **项目条目交互**：单击跳转项目任务页；左滑显示「重命名/删除」不跳转（SwipeItem 展开时点击关闭不冒泡）
- **任务条目标注**：无项目任务显示归属（无项目）+ 创建时间 +「加入日程」按钮；已安排日程的任务**灰色** + 「已安排」标记
- **加入日程**：点击跳日历页（`?task=`）自动选中该任务 + 提示「请选择一天」；点日期**打开确认弹窗**，确认后 toast「已添加」；**已安排任务不能重复添加**
- **日历弹窗防重**：安排弹窗的任务下拉框**过滤已安排任务**，不重复显示
- **复盘按天**：复盘页「天/周/月」三档，支持按天查看完成记录
- **过滤已完成**：今日页打钩完成的任务从任务列表消失（completion 判断）；只显示未完成任务
- **浏览器实测**：全部通过
- **修复（20:45）**：左滑展开后**不自动弹回**（区分拖动与点击，拖动后松手的 click 不触发关闭），需右滑或点击内容收回
- **修复（20:48/20:52）**：项目条目**右滑收回时不再误触发跳转**（capture 阶段拦截拖动后的 click，在条目 `@click` 之前阻止），仅单击进入项目页

### 十二.12、2026-08-03 21:40 P2 部署上线

- **PWA 图标**：sharp 生成黑白便签风格图标（`pwa-192/pwa-512/pwa-maskable-512/apple-touch-icon`），manifest 配置 icons + apple-touch-icon
- **GitHub Actions**：`.github/workflows/deploy.yml`（`vite build` → 部署到 GitHub Pages）
- **本地验证**：build 通过，manifest icons 完整，SW precache 14 项
- **待确认**：线上地址 `https://zhan-zip.github.io/Inspiration-Note/`（Actions 跑完后验证）

---

## 十三、测试记录

> 待实施后按测试记录补录。

（暂无）
