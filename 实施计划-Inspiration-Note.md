# Inspiration-Note — 实施计划

> **用途**：明确「从哪部分开始、做什么功能、怎么实现、按什么顺序、什么优先级、达到什么效果」，供实施时逐阶段执行与勾选。
>
> **关联文档**：`交接文档-Inspiration-Note.md`（需求、界面、导航、删除方案、技术栈、数据模型均已确认）

---

## 一、项目目标（回顾）

一款简约黑白的**个人灵感记录 + 任务管理 PWA**，解决「灵感随手记了不知道忘哪去」的痛点。核心闭环：

**记灵感 → 一键转任务 → 安排到日历（可周期） → 今日打钩完成 → 复盘看成就 → 任务量直观可视**

**导航**：主页=灵感页，左滑呼出页面导航列表切换（无底部 Tab）
**删除**：右滑删除 → 软删除进回收站 → 可恢复 / 彻底删除

---

## 二、技术栈（已确认 + 补充）

| 技术 | 用途 | 说明 |
|------|------|------|
| **Vue 3 + Vite** | 前端框架 | 用户已熟练掌握（食光记同栈） |
| **Pinia** | 状态管理 | 生态标配 |
| **vue-router** | 页面路由 | ⚠️ 补充依赖：页面切换需要 |
| **vite-plugin-pwa** | PWA 能力 | 可安装、离线缓存、添加到主屏 |
| **idb** | IndexedDB 封装 | ⚠️ 补充依赖：本地数据读写简洁可靠 |
| **纯 CSS 黑白主题** | 界面 | 以白为主，CSS 变量定义黑白灰阶 |
| **GitHub Pages** | 免费托管 | 需配 `base: '/Inspiration-Note/'` |

---

## 三、总体实施策略（顺序与优先级总览）

**顺序原则**：先搭骨架与地基 → 让「主线闭环」跑通 → 再做增强功能 → 最后上线。

**优先级定义**：

- **P0（核心主线）**：没有它闭环断掉，先做完
- **P1（增强体验）**：主线可用后的重要补充
- **P2（上线收尾）**：打磨与部署

| 阶段 | 优先级 | 内容 | 前置依赖 |
|------|--------|------|----------|
| 阶段 0 | **P0** | 项目初始化 + 基础骨架（PWA/base/黑白主题/**侧滑抽屉导航**） | 无 |
| 阶段 1 | **P0** | 数据层（IndexedDB 5 表 + 软删除字段 + Pinia store） | 阶段 0 |
| 阶段 2 | **P0** | 灵感页（默认主页） | 阶段 1 |
| 阶段 3 | **P0** | 任务列表页（待办池） | 阶段 1 |
| 阶段 4 | **P0** | 日历页 + 任务量可视化 | 阶段 1 |
| 阶段 5 | **P0** | 今日页（打钩完成） | 阶段 1、4 |
| 阶段 6 | **P1** | 复盘页（周/月 + 项目分类） | 阶段 1、5 |
| 阶段 7 | **P1** | 项目管理页（删除连带任务） | 阶段 1 |
| 阶段 8 | **P1** | 回收站页（恢复 / 彻底删除） | 阶段 1、2、3、7 |
| 阶段 9 | **P2** | PWA 完善 + Git 推送 + GitHub Pages 部署 | 全部 |

---

## 四、分阶段详细计划

### 阶段 0：项目初始化与基础骨架（P0）

**做什么**：把项目「壳」搭起来——脚手架、依赖、PWA/base 配置、黑白主题、**侧滑抽屉导航框架**。

**怎么实现**：
1. 在**当前目录** `D:\Desktop\test\claude-test\inspiration-note` 执行 `npm create vite@latest .`（Vue 模板）；目录已有文档，若提示非空选择「忽略现有文件」，保留交接文档与实施计划
2. 安装依赖：`npm i pinia vue-router vite-plugin-pwa idb`
3. 配置 `vite.config.js`：
   - `base: '/Inspiration-Note/'`（子路径部署，防静态资源 404）
   - `VitePWA({ manifest, workbox })`：应用名、黑白主题色、图标（阶段 9 补真图标）
4. 建立目录结构：`src/views`（7 个页面）、`src/components`、`src/stores`、`src/db`、`src/styles`
5. 黑白主题：`styles/theme.css` 定义 CSS 变量（`--bg:#fff; --fg:#111; --mid:#ccc; --dark:#000`），以白为主
6. **侧滑抽屉导航框架**（核心）：
   - 主页 = 灵感页；主视图整体可左右位移
   - 触摸**左滑** → 主视图右移，左侧**页面导航列表**滑入，列出 7 个页面名：灵感 / 任务列表 / 日历 / 今日 / 复盘 / 项目 / 回收站
   - **点击页面名** → 列表收回，主视图切换为对应页面
   - 从任意页面左滑均可呼出导航列表

**预期效果**：`npm run dev` 跑起来，黑白外壳成型，左滑呼出导航列表可切换 7 个页面占位。

---

### 阶段 1：数据层（IndexedDB 封装 + Pinia store）（P0）

**做什么**：让所有页面有数据可用，读写本地、刷新不丢，并支持软删除。

**怎么实现**：
1. 封装 `src/db/db.js`：打开数据库，按数据模型建 5 个 objectStore 并带索引：

   | store | 主键 | 关键字段 | 索引 |
   |-------|------|----------|------|
   | ideas | id | content, created_at, status, **deleted_at** | — |
   | projects | id | name, created_at, **deleted_at** | — |
   | tasks | id | name, project_id（可空）, status, created_at, **deleted_at** | project_id |
   | schedules | id | task_id, date, type（once/weekly/monthly/yearly） | task_id |
   | completions | id | task_id, date | task_id, date |

2. Pinia store（`src/stores/`）：`useIdeaStore / useProjectStore / useTaskStore / useScheduleStore / useCompletionStore`
3. **软删除规则统一封装在 store 层**：
   - 删除 = 把 `deleted_at` 置为当前时间（不是物理删）
   - 各查询方法统一过滤 `deleted_at = null`
   - `restore()` 恢复 = 清空 `deleted_at`；`purge()` 彻底删除 = 物理删
   - 项目删除连带其下任务一并软删除；项目恢复时任务一并恢复
   - 任务软删除后其 `schedules` / `completions` 保留但被过滤，恢复后生效

**预期效果**：任何页面读写本地数据，刷新不丢；删除/恢复/彻底删除全部走统一封装。

---

### 阶段 2：灵感页（P0 · 默认主页 / 开屏）

**做什么**：开屏即记灵感，下拉看全部记录，一键转任务，右滑删除。

**怎么实现**：
1. 顶部一行大字提问：「**记录一个灵感吗？**」
2. 中间输入框：输入内容提交 → 写入 idea store（status=未转）
3. **下拉手势** → 展开全部灵感记录列表（按创建时间倒序，未转优先）；**上拉手势** → 收起回到记录输入框
4. 每条灵感可「**转为任务**」→ 弹出选择：归入**已有项目**（下拉选）／**新建项目**（输入项目名）／**无项目** → 创建 task 并标记灵感已转
5. **右滑删除**：每条灵感右滑露出删除键 → 点击 → 弹确认 → 软删除进回收站

**预期效果**：打开 App 就能记；下拉即查全部灵感；灵感到任务 3 步内完成。

---

### 阶段 3：任务列表页（P0 · 待办池）

**做什么**：管理所有**未安排时间**的任务。

**怎么实现**：
1. 列出 status=待办 且未被日历安排、且未软删除的任务
2. 支持：新增任务（输入名称，可选归入项目）、编辑名称、标记完成
3. **右滑删除** → 弹确认 → 软删除进回收站

**预期效果**：待办池清晰可见，任务可增删改。

---

### 阶段 4：日历页 + 任务量可视化（P0）

**做什么**：把任务安排到某天（支持周期），并用颜色直观展示每天任务量。

**怎么实现**：
1. 手写月历组件：当月 28~31 天网格，可上/下月切换，可回今天
2. 点击某天 → 弹窗：勾选要安排的任务 + 选择周期类型（**单次 / 每周 / 每月 / 每年**）→ 写入 schedule store
3. **任务量可视化**：统计每天任务数（排除软删除任务）→ 背景色映射：**≤2 白、3~4 灰、≥5 黑**（用户定 3 灰 / 5 黑）
4. 点击某天查看当天已安排任务，支持删除该安排

**预期效果**：一眼看出「哪天累不累」；任务安排/取消都在日历上完成。

---

### 阶段 5：今日页（P0 · 当天任务打钩）

**做什么**：每天打开，看到今天该做的任务，打钩完成。

**怎么实现**：
1. 聚合「今天应安排的任务」（排除软删除任务）：
   - 单次（once）：date = 今天
   - 每周（weekly）：星期几 = 今天星期几
   - 每月（monthly）：日期 = 今天日期
   - 每年（yearly）：月日 = 今天月日
2. 列表展示今天任务；打钩 → 写入 completion（task_id + date）→ 回显已完成状态
3. 可取消打钩（删除对应 completion）

**预期效果**：每天任务清单自动生成，打钩即完成，沉淀完成记录供复盘。

---

### 阶段 6：复盘页（P1）

**做什么**：按周/月回看已完成任务，获得成就感。

**怎么实现**：
1. 周/月视图切换（本周/上一周…、本月/上一个月…）
2. 取该周期内全部 completion → 对应任务（排除软删除）
3. **按项目分类**展示（无项目任务归入「无项目」）
4. 每个任务下方一行**小字角标**标注所属项目名（如「食光记」）

**预期效果**：复盘「这周/这月完成了什么」，成就感拉满。

---

### 阶段 7：项目管理页（P1）

**做什么**：项目全生命周期管理 + 项目内任务管理。

**怎么实现**：
1. 项目以**区块/卡片**展示，点击项目 → 展开项目内具体任务
2. **无项目**任务单独一列放在项目下方
3. 支持**增删改移**：
   - 增：新建项目；项目内新增任务
   - 删：删除项目 / 删除任务（右滑 + 弹确认 + 软删除）
   - 改：项目重命名、任务改名
   - 移：任务移动到其他项目 / 移动到无项目
4. **删除项目**：弹提示「**将连带删除其下所有任务**」+ 建议先将有用任务移出 → 确认后项目与其下所有任务一并软删除进回收站

**预期效果**：项目井井有条，任务归属随时可调整。

---

### 阶段 8：回收站页（P1 · 2026-08-02 新增）

**做什么**：集中管理所有软删除内容，可恢复或彻底删除。

**怎么实现**：
1. 列出全部已软删除内容（灵感 / 任务 / 项目）+ **删除时间**，按类型分组展示
2. **恢复**：清空 `deleted_at` 回原位；项目恢复时其下任务一并恢复
3. **彻底删除**：物理从 IndexedDB 删除，**不可恢复**（删除前弹确认）
4. 空状态提示「回收站是空的」

**预期效果**：误删可救回；确定不要的可物理清除。

---

### 阶段 9：PWA 完善 + Git 推送 + 部署上线（P2）

**做什么**：让应用真正能装到手机、离线可用。

**怎么实现**：
1. 生成 PWA 图标（192×192 / 512×512 / maskable，黑白风格）
2. 本地验证：`npm run build` + `npm run preview`，确认 service worker 离线缓存生效、可添加到主屏
3. 按 Git 规则推送（先同步文档 → 检查改动 → 提交 → 推送）：
   - 新建 `.gitignore`（排除 node_modules、dist 等）
   - `git init` → `git remote add origin https://github.com/zhan-zip/Inspiration-Note.git`
   - 提交（中文描述）→ `git push origin main`（或 master，视仓库默认分支）
4. 配置 GitHub Actions：`.github/workflows/deploy.yml`（`vite build` → 部署到 Pages）
5. 验证线上地址 `https://zhan-zip.github.io/Inspiration-Note/`

**预期效果**：手机浏览器打开 → 添加到主屏 → 离线使用，完成「纯本地个人工具」闭环。

---

## 五、优先级说明

- **P0 先做**（阶段 0~5）：核心主线「灵感→任务→日历→今日」先跑通，任何一步断了用户就无法正常用
- **P1 其次**（阶段 6~8）：复盘、项目管理、回收站是体验增强，主线可用后立刻补上
- **P2 收尾**（阶段 9）：部署上线，功能全好之后再做

> 每个阶段都独立可验证（`npm run dev` 自测），即使中途暂停，已完成阶段也立即可用。

---

## 六、完成标准 / 验收方式

- **每阶段**：`npm run dev` 起本地服务，逐项自测该阶段功能（自动化优先，涉及浏览器交互时请用户配合）
- **最终验收清单**：
  - [ ] 主页左滑呼出导航列表，点击切换 7 个页面；从任意页可呼出
  - [ ] 灵感页开屏即记，下拉展开记录 / 上拉收起，一键转任务（选项目/无项目）
  - [ ] 日历安排任务（单次 + 每周/每月/每年），颜色白/灰/黑正确（3 灰 5 黑）
  - [ ] 今日页自动列出当天任务（含周期任务），打钩完成
  - [ ] 复盘页周/月可看已完成，按项目分类带角标
  - [ ] 项目管理页增删改移可用；删除项目有连带提示
  - [ ] 各列表右滑可删除，弹确认后进回收站
  - [ ] 回收站可恢复 / 彻底删除；项目恢复时任务一并恢复
  - [ ] 手机上可添加到主屏、离线可记灵感
  - [ ] Pages 地址 `https://zhan-zip.github.io/Inspiration-Note/` 可访问

---

## 七、风险与注意事项

| 风险 | 说明 | 对策 |
|------|------|------|
| **base 路径 404** | Pages 部署在子路径，资源路径不对会全白 | 一开始就配 `base: '/Inspiration-Note/'`，不事后补 |
| **目录非空初始化** | 当前目录已有文档，脚手架可能提示 | 选「忽略现有文件」，保留 md 文档 |
| **侧滑手势与页面滚动冲突** | 灵感页下拉展开与页面滚动易冲突 | 手势区域/阈值需单独调测（阶段 2 重点测试） |
| **IndexedDB 结构变更** | 后续改数据模型需升版本号 | db.js 统一管理版本与迁移 |
| **周期任务日期匹配** | 每周/每月/每年的匹配逻辑易错 | 单独封装 `getTasksForDate()` 统一处理并测试 |
| **软删除过滤遗漏** | 各页面查询需统一过滤 `deleted_at` | store 层统一封装，页面不直接碰 DB |
| **PWA 图标缺失** | 无图标无法添加到主屏 | 阶段 9 补真图标再部署 |

---

## 八、任务清单（实施时逐项勾选）

- [x] 阶段 0：项目初始化 + 基础骨架（侧滑抽屉导航）
- [x] 阶段 1：数据层（IndexedDB 5 表 + 软删除 + Pinia）
- [x] 阶段 2：灵感页（下拉/上拉 + 转任务 + 右滑删除）
- [x] 阶段 3：任务列表页（右滑删除）
- [x] 阶段 4：日历页 + 任务量可视化
- [x] 阶段 5：今日页
- [x] 阶段 6：复盘页
- [x] 阶段 7：项目管理页（删除连带任务）
- [x] 阶段 8：回收站页（恢复 / 彻底删除）
- [ ] 阶段 9：PWA 完善 + 部署上线

---

**最后更新：** 2026-08-03 19:18
**进度：** P0（阶段 0~5）、P1（阶段 6~8）完成；前端优化第一批（P0 三项）已完成并通过浏览器自测（见《前端优化计划》）；P2（阶段 9）待实施
